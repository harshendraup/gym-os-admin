services:
  admin:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: gymos_admin
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:3333/api/v1
    volumes:
      - .:/app
      - /app/node_modules
